import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
	colors: {
		brand: {
			900: "93A4FF", // vista blue
			800: "#C2DDFD", // uranian blue
			700: "#6065EA", //neon blue
		},
	},
});

export default theme;
